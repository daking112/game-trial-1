# World / region key art

Drop the Gearwood Thicket background as `gearwood-thicket.png` here (a wide
key-art image works well as a Phaser background texture behind the
procedural grid in `BattleScene.drawMap()`). See `../monsters/README.md`
for context on why this isn't already populated.
